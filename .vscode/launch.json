{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "PowerShell",
            "request": "launch",
            "name": "Build",
            "script": "${workspaceRoot}\\build.ps1 build",
            "args": [],
            "cwd": "${workspaceRoot}"
        }
    ]
}
